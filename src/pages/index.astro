---
// index.astro
import Layout from "../layouts/Layout.astro";
---

<Layout>
	<div class="page">
		<!-- Hero Section -->
		<section class="hero">
			<div class="hero-image">
				<img src="/images/hero.png" alt="Industrial Property" />
			</div>
			<div class="hero-content">
				<h1 class="hero-title">
					The Nation's Leading Buyer of Industrial Real Estate
				</h1>
				<div class="invest-arrow">
					<p class="hero-subtitle">Invest. Elevate. Repeat.</p>
					<button
						class="scroll-arrow"
						aria-label="Scroll to next section"
					>
						<img src="/images/down-arrow.svg" alt="down arrow" />
					</button>
				</div>
			</div>
		</section>

		<!-- Tagline Section -->
		<section class="tagline-section" id="next-section">
			<div class="container">
				<h2 class="tagline">Small Bay Buyers Doing Big Things</h2>
			</div>
		</section>

		<!-- Stats Section -->
		<section class="stats-section">
			<div class="stats-container">
				<div class="stat-card">
					<div class="stat-number">
						3,000,000 <span class="additional">SF+</span>
					</div>
					<div class="stat-description">UNDER MANAGEMENT</div>
				</div>
				<div class="stat-divider"></div>
				<div class="stat-card">
					<div class="stat-number">
						$200<span class="additional">M</span>
					</div>
					<div class="stat-description">EQUITY DEPLOYED</div>
				</div>
				<div class="stat-divider"></div>
				<div class="stat-card">
					<div class="stat-number">
						15<span class="additional">+</span>
					</div>
					<div class="stat-description">
						YEARS OF REAL ESTATE EXPERTISE
					</div>
				</div>
			</div>
		</section>

		<!-- Portfolio Showcase -->
		<section class="portfolio-showcase">
			<div class="container">
				<div class="portfolio-grid">
					<div class="portfolio-card">
						<div class="portfolio-image">
							<img src="/images/property1.png" alt="Property 1" />
						</div>
						<div class="portfolio-label">Property 1</div>
					</div>
					<div class="portfolio-card">
						<div class="portfolio-image">
							<img src="/images/property2.png" alt="Property 2" />
						</div>
						<div class="portfolio-label">Property 2</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Investment Approach -->
		<section class="investment-section">
			<div class="investment-wrapper">
				<div class="investment-content">
					<h2 class="section-title">
						Small-Bay Industrial Real Estate: The Investment Appeal
					</h2>
					<p class="section-description">
						Small-bay industrial properties typically 1,000-10,000
						square feet. Decker Capital specializes in the
						1,000-25,000 SF range. These spaces serve light
						manufacturing, warehousing, distribution, and diverse
						uses such as e-commerce firms, contractors, and
						landscapers, with demand rising due to economic and
						geographic factors favoring sustainable supply.
					</p>
				</div>
				<div class="investment-image">
					<img
						src="/images/property-below.png"
						alt="Investment Property"
					/>
				</div>
			</div>
		</section>

		<!-- Value Props -->
		<section class="value-props-section">
			<div class="container">
				<div class="value-props-grid">
					<div class="value-prop-card">
						<h3 class="value-prop-title">
							Why Small Bay & Investment Criteria
						</h3>
						<p class="value-prop-text">
							Strategic focus on high-demand industrial spaces
							that serve essential business needs.
						</p>
					</div>
					<div class="value-prop-card">
						<h3 class="value-prop-title">
							How We've Become A Leader In The Space
						</h3>
						<p class="value-prop-text">
							Proven track record of identifying value and
							creating exceptional returns for our investors.
						</p>
					</div>
					<div class="value-prop-card">
						<h3 class="value-prop-title">
							Our Team & Company Culture
						</h3>
						<p class="value-prop-text">
							Experienced professionals united by a commitment to
							excellence and integrity.
						</p>
					</div>
				</div>
			</div>
		</section>

		<!-- Portfolio Slideshow -->
		<section class="slideshow-section">
			<div class="slideshow-container">
				<div class="slideshow-header">
					<span class="slideshow-eyebrow">Our Work</span>
					<h2 class="slideshow-title">Portfolio Highlights</h2>
				</div>
				
				<div class="slideshow-wrapper">
					<div class="slideshow-main">
						<div class="slide-container">
							<div class="slide active" data-index="0">
								<img src="/images/property1.png" alt="Industrial Property - Modern Warehouse Facility" />
								<div class="slide-overlay"></div>
							</div>
							<div class="slide" data-index="1">
								<img src="/images/property2.png" alt="Industrial Property - Distribution Center" />
								<div class="slide-overlay"></div>
							</div>
							<div class="slide" data-index="2">
								<img src="/images/hero.png" alt="Industrial Property - Multi-Tenant Complex" />
								<div class="slide-overlay"></div>
							</div>
						</div>
						
						<div class="slide-info">
							<div class="slide-content active" data-index="0">
								<span class="slide-number">01</span>
								<h3 class="slide-name">Westfield Industrial Park</h3>
								<p class="slide-details">125,000 SF · Phoenix, AZ</p>
							</div>
							<div class="slide-content" data-index="1">
								<span class="slide-number">02</span>
								<h3 class="slide-name">Metro Distribution Center</h3>
								<p class="slide-details">89,000 SF · Dallas, TX</p>
							</div>
							<div class="slide-content" data-index="2">
								<span class="slide-number">03</span>
								<h3 class="slide-name">Summit Business Complex</h3>
								<p class="slide-details">210,000 SF · Atlanta, GA</p>
							</div>
						</div>
					</div>
					
					<div class="slideshow-controls">
						<div class="slide-indicators">
							<button class="indicator active" data-index="0" aria-label="View slide 1">
								<span class="indicator-progress"></span>
							</button>
							<button class="indicator" data-index="1" aria-label="View slide 2">
								<span class="indicator-progress"></span>
							</button>
							<button class="indicator" data-index="2" aria-label="View slide 3">
								<span class="indicator-progress"></span>
							</button>
						</div>
						
						<div class="slide-nav">
							<button class="nav-btn prev" aria-label="Previous slide">
								<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
									<path d="M19 12H5M12 19l-7-7 7-7"/>
								</svg>
							</button>
							<button class="nav-btn next" aria-label="Next slide">
								<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
									<path d="M5 12h14M12 5l7 7-7 7"/>
								</svg>
							</button>
						</div>
					</div>
				</div>
			</div>
		</section>
	</div>
</Layout>

<script>
	// Smooth scroll functionality for arrow
	document.addEventListener("DOMContentLoaded", () => {
		const scrollArrow = document.querySelector(".scroll-arrow");
		const nextSection = document.getElementById("next-section");

		if (scrollArrow && nextSection) {
			scrollArrow.addEventListener("click", () => {
				nextSection.scrollIntoView({
					behavior: "smooth",
					block: "start",
				});
			});
		}
	});
</script>

<script>
document.addEventListener('DOMContentLoaded', () => {
	const slides = document.querySelectorAll('.slide');
	const slideContents = document.querySelectorAll('.slide-content');
	const indicators = document.querySelectorAll('.indicator');
	const prevBtn = document.querySelector('.nav-btn.prev');
	const nextBtn = document.querySelector('.nav-btn.next');
	
	let currentIndex = 0;
	let autoplayInterval: ReturnType<typeof setInterval>;
	const autoplayDuration = 5000;
	let isPaused = false;
	
	function updateSlide(newIndex: number) {
		// Remove active states
		slides.forEach(slide => slide.classList.remove('active', 'prev'));
		slideContents.forEach(content => content.classList.remove('active'));
		indicators.forEach(ind => {
			ind.classList.remove('active');
			const progress = ind.querySelector('.indicator-progress') as HTMLElement;
			if (progress) {
				progress.style.animation = 'none';
				progress.offsetHeight; // Trigger reflow
				progress.style.animation = '';
			}
		});
		
		// Mark previous slide
		slides[currentIndex]?.classList.add('prev');
		
		// Set new active states
		currentIndex = newIndex;
		slides[currentIndex]?.classList.add('active');
		slideContents[currentIndex]?.classList.add('active');
		indicators[currentIndex]?.classList.add('active');
	}
	
	function nextSlide() {
		const newIndex = (currentIndex + 1) % slides.length;
		updateSlide(newIndex);
	}
	
	function prevSlide() {
		const newIndex = (currentIndex - 1 + slides.length) % slides.length;
		updateSlide(newIndex);
	}
	
	function startAutoplay() {
		stopAutoplay();
		autoplayInterval = setInterval(() => {
			if (!isPaused) nextSlide();
		}, autoplayDuration);
	}
	
	function stopAutoplay() {
		if (autoplayInterval) clearInterval(autoplayInterval);
	}
	
	// Event listeners
	prevBtn?.addEventListener('click', () => {
		prevSlide();
		startAutoplay();
	});
	
	nextBtn?.addEventListener('click', () => {
		nextSlide();
		startAutoplay();
	});
	
	indicators.forEach((indicator, index) => {
		indicator.addEventListener('click', () => {
			if (index !== currentIndex) {
				updateSlide(index);
				startAutoplay();
			}
		});
	});
	
	// Pause on hover
	const slideshowWrapper = document.querySelector('.slideshow-wrapper');
	slideshowWrapper?.addEventListener('mouseenter', () => isPaused = true);
	slideshowWrapper?.addEventListener('mouseleave', () => isPaused = false);
	
	// Keyboard navigation
	document.addEventListener('keydown', (e) => {
		if (e.key === 'ArrowLeft') {
			prevSlide();
			startAutoplay();
		} else if (e.key === 'ArrowRight') {
			nextSlide();
			startAutoplay();
		}
	});
	
	// Start autoplay
	startAutoplay();
});
</script>

<style>
	/* ===== CSS Variables for Consistent Spacing ===== */
	:root {
		--padding-desktop: 60px;
		--padding-tablet: 40px;
		--padding-mobile: 24px;
		--section-padding-y: 80px;
		--section-padding-y-mobile: 60px;
		--max-width: 1400px;
	}

	.page {
		background: #ffffff;
	}

	.container {
		max-width: var(--max-width);
		margin: 0 auto;
		padding: 0 var(--padding-desktop);
	}

	/* Hero Section */
	.hero {
		position: relative;
		width: 100%;
		height: 100vh;
		min-height: 600px;
		overflow: hidden;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.hero-image {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		z-index: 1;
	}

	.hero-image::after {
		content: "";
		position: absolute;
		inset: 0;
		background: linear-gradient(
			to bottom,
			rgba(0, 0, 0, 0.65) 0%,
			rgba(0, 0, 0, 0.45) 40%,
			rgba(0, 0, 0, 0.65) 100%
		);
		z-index: 2;
	}

	.hero-image img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		object-position: center;
	}

	.hero-content {
		position: relative;
		z-index: 3;
		max-width: 1200px;
		text-align: center;
		padding: 0 var(--padding-desktop);
		display: flex;
		flex-direction: column;
		align-items: center;
	}

	.hero-title {
		font-family: "Cormorant Garamond", serif;
		font-size: 72px;
		font-weight: 400;
		line-height: 1.2;
		color: #ffffff;
		letter-spacing: -0.5px;
		text-shadow: 0 2px 20px rgba(0, 0, 0, 0.3);
	}

	.invest-arrow {
		top: 175px;
		position: relative;
	}

	.hero-subtitle {
		font-family: "Inter", sans-serif;
		font-size: 16px;
		font-weight: 400;
		letter-spacing: 3px;
		text-transform: uppercase;
		color: #ffffff;
		margin: 48px 0 0 0;
		text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
	}

	.scroll-arrow {
		background: none;
		border: none;
		color: #ffffff;
		cursor: pointer;
		padding: 20px;
		transition: all 0.3s ease-in-out;
	}

	.scroll-arrow:hover {
		transform: translateY(4px);
		opacity: 0.8;
	}

	.scroll-arrow svg {
		display: block;
		width: 32px;
		height: 32px;
		filter: drop-shadow(0 2px 10px rgba(0, 0, 0, 0.3));
	}

	/* Tagline Section */
	.tagline-section {
		padding: 20px var(--padding-desktop);
		background: #ffffff;
	}

	.tagline-section .container {
		padding: 0;
	}

	.tagline {
		font-family: "Cormorant Garamond", serif;
		font-size: 48px;
		font-weight: 400;
		line-height: 1.3;
		color: #1a1a1a;
		margin: 0;
		text-align: left;
	}

	/* Stats Section */
	.stats-section {
		padding: 0;
		background: #dbd3c6;
	}

	.stats-container {
		display: flex;
		align-items: stretch;
		justify-content: space-between;
		max-width: 100%;
	}

	.stat-card {
		flex: 1;
		text-align: center;
		padding: 60px 40px;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
	}

	.stat-divider {
		width: 1px;
		background: #000000;
	}

	.stat-number {
		font-family: "Cormorant Garamond", serif;
		font-size: 64px;
		font-weight: 400;
		color: #1a1a1a;
		line-height: 1;
		display: inline;
	}

	.stat-number .additional {
		font-size: 40px;
	}

	.stat-label {
		font-family: "Cormorant Garamond", serif;
		font-size: 64px;
		font-weight: 400;
		color: #1a1a1a;
		display: inline;
		margin-left: 4px;
	}

	.stat-description {
		font-family: "Inter", sans-serif;
		font-size: 13px;
		font-weight: 400;
		letter-spacing: 1.5px;
		color: #1a1a1a;
		text-transform: uppercase;
		margin-top: 16px;
	}

	/* Portfolio Showcase */
	.portfolio-showcase {
		padding: 20px var(--padding-desktop);
		background: #ffffff;
		border-bottom: 1px solid black;
	}

	.portfolio-showcase .container {
		padding: 0;
	}

	.portfolio-grid {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 20px;
	}

	.portfolio-card {
		position: relative;
		overflow: hidden;
		cursor: pointer;
	}

	.portfolio-image {
		aspect-ratio: 16/10;
		overflow: hidden;
	}

	.portfolio-image img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		transition: transform 0.6s ease-in-out;
	}

	.portfolio-card:hover .portfolio-image img {
		transform: scale(1.05);
	}

	.portfolio-label {
		font-family: "Inter", sans-serif;
		font-size: 12px;
		font-weight: 500;
		letter-spacing: 1.5px;
		text-transform: uppercase;
		color: #1a1a1a;
		margin-top: 12px;
	}

	/* Investment Section */
	.investment-section {
		padding: 80px 0 0 0;
	}

	.investment-wrapper {
		display: flex;
		flex-direction: column;
		position: relative;
	}

	.investment-content {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 60px;
		padding: 0 var(--padding-desktop) 60px;
		align-items: start;
		z-index: 3;
	}

	.section-title {
		font-family: "Cormorant Garamond", serif;
		font-size: 42px;
		font-weight: 400;
		line-height: 1.3;
		color: #1a1a1a;
		margin: 0;
	}

	.section-description {
		font-family: "Inter", sans-serif;
		font-size: 16px;
		font-weight: 400;
		line-height: 1.8;
		color: #333;
		margin: 0;
	}

	.investment-image {
		width: 100%;
	}

	.investment-image img {
		width: 100%;
		object-fit: cover;
		object-position: center;
	}

	/* Value Props */
	.value-props-section {
		padding: var(--section-padding-y) var(--padding-desktop);
		background: #ffffff;
		position: relative;
		z-index: 3;
	}

	.value-props-section .container {
		padding: 0;
	}

	.value-props-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 30px;
	}

	.value-prop-card {
		padding: 50px 35px;
		background: #dbd3c6;
		border: 1px solid rgba(0, 0, 0, 0.04);
		transition: all 0.4s ease-in-out;
	}

	.value-prop-card:hover {
		transform: translateY(-4px);
		box-shadow: 0 12px 40px rgba(0, 0, 0, 0.08);
	}

	.value-prop-title {
		font-family: "Cormorant Garamond", serif;
		font-size: 26px;
		font-weight: 500;
		line-height: 1.4;
		color: #1a1a1a;
		margin: 0 0 20px 0;
	}

	.value-prop-text {
		font-family: "Inter", sans-serif;
		font-size: 15px;
		font-weight: 400;
		line-height: 1.7;
		color: #666;
		margin: 0;
	}

	/* Slideshow Section */
	.slideshow-section {
		padding: var(--section-padding-y) var(--padding-desktop);
		background: linear-gradient(180deg, #dbd3c6 0%, #f0ede6 100%);
		position: relative;
		overflow: hidden;
	}

	.slideshow-section::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 1px;
		background: linear-gradient(90deg, transparent, rgba(0,0,0,0.08), transparent);
	}

	.slideshow-container {
		max-width: var(--max-width);
		margin: 0 auto;
	}

	.slideshow-header {
		text-align: center;
		margin-bottom: 50px;
	}

	.slideshow-eyebrow {
		font-family: 'Inter', sans-serif;
		font-size: 11px;
		font-weight: 500;
		letter-spacing: 3px;
		text-transform: uppercase;
		color: #8a7f6e;
		display: block;
		margin-bottom: 16px;
	}

	.slideshow-title {
		font-family: 'Cormorant Garamond', serif;
		font-size: 52px;
		font-weight: 400;
		color: #1a1a1a;
		margin: 0;
		letter-spacing: -0.5px;
	}

	.slideshow-wrapper {
		position: relative;
	}

	.slideshow-main {
		position: relative;
	}

	.slide-container {
		position: relative;
		width: 100%;
		aspect-ratio: 16/9;
		overflow: hidden;
		background: #1a1a1a;
	}

	.slide {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		opacity: 0;
		visibility: hidden;
		transition: opacity 0.8s cubic-bezier(0.4, 0, 0.2, 1), 
					visibility 0.8s cubic-bezier(0.4, 0, 0.2, 1);
	}

	.slide.active {
		opacity: 1;
		visibility: visible;
		z-index: 2;
	}

	.slide.prev {
		z-index: 1;
	}

	.slide img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		object-position: center;
		transform: scale(1);
		transition: transform 6s cubic-bezier(0.4, 0, 0.2, 1);
	}

	.slide.active img {
		transform: scale(1.05);
	}

	.slide-overlay {
		position: absolute;
		inset: 0;
		background: linear-gradient(
			to top,
			rgba(0, 0, 0, 0.5) 0%,
			rgba(0, 0, 0, 0) 40%,
			rgba(0, 0, 0, 0) 60%,
			rgba(0, 0, 0, 0.2) 100%
		);
		pointer-events: none;
	}

	.slide-info {
		position: absolute;
		bottom: 0;
		left: 0;
		right: 0;
		padding: 40px;
		z-index: 10;
	}

	.slide-content {
		opacity: 0;
		transform: translateY(20px);
		position: absolute;
		bottom: 40px;
		left: 40px;
		transition: opacity 0.6s ease, transform 0.6s ease;
		transition-delay: 0s;
	}

	.slide-content.active {
		opacity: 1;
		transform: translateY(0);
		transition-delay: 0.3s;
	}

	.slide-number {
		font-family: 'Cormorant Garamond', serif;
		font-size: 14px;
		font-weight: 400;
		color: rgba(255, 255, 255, 0.6);
		display: block;
		margin-bottom: 8px;
		letter-spacing: 2px;
	}

	.slide-name {
		font-family: 'Cormorant Garamond', serif;
		font-size: 32px;
		font-weight: 400;
		color: #ffffff;
		margin: 0 0 8px 0;
		letter-spacing: -0.3px;
		text-shadow: 0 2px 20px rgba(0, 0, 0, 0.3);
	}

	.slide-details {
		font-family: 'Inter', sans-serif;
		font-size: 13px;
		font-weight: 400;
		color: rgba(255, 255, 255, 0.8);
		margin: 0;
		letter-spacing: 1px;
	}

	.slideshow-controls {
		display: flex;
		align-items: center;
		justify-content: space-between;
		margin-top: 30px;
	}

	.slide-indicators {
		display: flex;
		gap: 12px;
	}

	.indicator {
		width: 60px;
		height: 3px;
		background: rgba(0, 0, 0, 0.12);
		border: none;
		padding: 0;
		cursor: pointer;
		position: relative;
		overflow: hidden;
		transition: background 0.3s ease;
	}

	.indicator:hover {
		background: rgba(0, 0, 0, 0.2);
	}

	.indicator-progress {
		position: absolute;
		top: 0;
		left: 0;
		height: 100%;
		width: 0;
		background: #1a1a1a;
		transition: width 0.3s ease;
	}

	.indicator.active .indicator-progress {
		animation: progress 5s linear forwards;
	}

	@keyframes progress {
		from { width: 0; }
		to { width: 100%; }
	}

	.slide-nav {
		display: flex;
		gap: 8px;
	}

	.nav-btn {
		width: 52px;
		height: 52px;
		border: 1px solid rgba(0, 0, 0, 0.15);
		background: transparent;
		cursor: pointer;
		display: flex;
		align-items: center;
		justify-content: center;
		color: #1a1a1a;
		transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
	}

	.nav-btn:hover {
		background: #1a1a1a;
		color: #ffffff;
		border-color: #1a1a1a;
	}

	.nav-btn svg {
		transition: transform 0.3s ease;
	}

	.nav-btn.prev:hover svg {
		transform: translateX(-3px);
	}

	.nav-btn.next:hover svg {
		transform: translateX(3px);
	}

	/* ===== Tablet Responsive ===== */
	@media (max-width: 1024px) {
		.hero-title {
			font-size: 56px;
		}

		.tagline-section {
			padding: 20px var(--padding-tablet);
		}

		.portfolio-showcase {
			padding: 20px var(--padding-tablet);
		}

		.investment-content {
			padding: 0 var(--padding-tablet) 50px;
		}

		.value-props-section {
			padding: 60px var(--padding-tablet);
		}

		.value-props-grid {
			grid-template-columns: 1fr;
			gap: 24px;
		}

		.portfolio-grid {
			grid-template-columns: 1fr;
			gap: 16px;
		}

		.investment-content {
			grid-template-columns: 1fr;
			gap: 40px;
		}

		.slideshow-section {
			padding: 60px var(--padding-tablet);
		}

		.slideshow-title {
			font-size: 42px;
		}

		.slide-name {
			font-size: 26px;
		}
	}

	/* ===== Mobile Responsive ===== */
	@media (max-width: 768px) {
		.hero {
			min-height: 500px;
		}

		.hero-content {
			padding: 0 var(--padding-mobile);
		}

		.hero-title {
			font-size: 36px;
		}

		.hero-subtitle {
			font-size: 12px;
			margin-top: 32px;
		}

		.invest-arrow {
			top: 120px;
		}

		.scroll-arrow {
			margin-top: 40px;
			padding: 15px;
		}

		.scroll-arrow svg {
			width: 24px;
			height: 24px;
		}

		.tagline-section {
			padding: 16px var(--padding-mobile);
		}

		.tagline {
			font-size: 28px;
		}

		.stats-container {
			flex-direction: column;
		}

		.stat-divider {
			width: 100%;
			height: 1px;
		}

		.stat-card {
			padding: 40px var(--padding-mobile);
		}

		.stat-number {
			font-size: 48px;
		}

		.portfolio-showcase {
			padding: 16px var(--padding-mobile);
		}

		.portfolio-grid {
			gap: 12px;
		}

		.portfolio-label {
			margin-top: 10px;
			font-size: 11px;
		}

		.investment-section {
			padding: var(--section-padding-y-mobile) 0 0 0;
		}

		.investment-content {
			padding: 0 var(--padding-mobile) 40px;
			gap: 24px;
		}

		.section-title {
			font-size: 28px;
		}

		.section-description {
			font-size: 15px;
		}

		.value-props-section {
			padding: var(--section-padding-y-mobile) var(--padding-mobile);
		}

		.value-prop-card {
			padding: 40px 28px;
		}

		.value-prop-title {
			font-size: 22px;
		}

		.value-prop-text {
			font-size: 14px;
		}

		.slideshow-section {
			padding: var(--section-padding-y-mobile) var(--padding-mobile);
		}

		.slideshow-header {
			margin-bottom: 32px;
		}

		.slideshow-title {
			font-size: 32px;
		}

		.slide-container {
			aspect-ratio: 4/3;
		}

		.slide-info {
			padding: 24px;
		}

		.slide-content {
			bottom: 24px;
			left: 24px;
		}

		.slide-name {
			font-size: 20px;
		}

		.slide-details {
			font-size: 12px;
		}

		.slideshow-controls {
			flex-direction: column-reverse;
			gap: 20px;
			margin-top: 20px;
		}

		.indicator {
			width: 40px;
		}

		.nav-btn {
			width: 46px;
			height: 46px;
		}
	}
</style>

<style is:global>
	::selection {
		background: #1a1a1a;
		color: #ffffff;
	}
</style>